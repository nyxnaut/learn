#include <stdio.h>

int main() {
	int x, y;
	printf("Enter two integers\n");
	scanf("%d %d", &x, &y);
	printf("The sum of your ints is %d\n", x+y);
	printf("The product of your ints is %d\n", x*y);
	return 0;
}
